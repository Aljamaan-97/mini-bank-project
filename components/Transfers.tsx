import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Transfer = () => {
  return (
    <View
      style={{
        height: 50,
        width: 400,
        backgroundColor: "orange",
        marginLeft: 0,
        marginTop: 10,
      }}
    >
      <Text style={{ marginLeft: 100 }}>Transfer amount</Text>
    </View>
  );
};

export default Transfer;

const styles = StyleSheet.create({});

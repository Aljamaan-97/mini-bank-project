import React from "react";
import { StyleSheet, Text, View } from "react-native";

const user = () => {
  return (
    <View>
      <Text>user</Text>
    </View>
  );
};

export default user;

const styles = StyleSheet.create({});
